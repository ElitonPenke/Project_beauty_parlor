import { Button } from '@mui/material';

export function ButtonChanceTheme(){
    return (
        <Button  color = "primary" variant = "contained" > {/*variant define o estilo do botão, pode ser tbm ouylined ou text*/ }
    LOGIN
        </Button>)
}

